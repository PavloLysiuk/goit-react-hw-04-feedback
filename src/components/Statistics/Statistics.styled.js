import { styled } from 'styled-components';

export const StatisticsItem = styled.li`
  margin-bottom: 8px;
  text-align: center;
  text-transform: uppercase;

  &:last-child {
    margin-bottom: 0;
    color: #40bfff;
    font-weight: 700;
  }
`;
